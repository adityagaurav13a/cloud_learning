## 🔧 Environment Setup

To run this project locally, create a Python virtual environment and install the required dependencies.

### 1. Create a virtual environment
On Linux/macOS/WSL:
```bash
python3 -m venv venv
source venv/bin/activate
```

### 2. Install Dependencies
```
pip install -r requirements.txt
```

### 3. Verify installation
```
pip list
```

### 4. Deactivate environment
When you’re done working:
```
deactivate
```

### NOTE :   - If you add new dependencies update the file with:
```
pip freeze > requirements.txt
```

